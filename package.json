{
  "private": true,
  "scripts": {
    "storybook": "turbo run storybook",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "config": "*",
    "eslint": "^9.5.0",
    "prettier": "^3.3.2",
    "turbo": "^2.0.4"
  },
  "name": "tristanbrattinga",
  "packageManager": "yarn@4.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "ui": "*"
  }
}
